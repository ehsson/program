#include <stdio.h>

long long sum(int * a, int n) 
{
	int tot = 0;
	
	for(int k = 0; k < n; k++) {
		scanf("%d", &a[k]);
		tot += a[k];
	}
	
	return tot;
}

int main(void)
{
	int n;
	int a[1000000];
	
	scanf("%d", &n);
	
	printf("%lld\n", sum(a, n));
	
	return 0;
}
