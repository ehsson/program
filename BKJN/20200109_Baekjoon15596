#include <stdio.h>

long long sum(int * a, int n) 
{
	int tot = 0;
	
	for(int k = 0; k < n; k++)
		scanf("%d", &a[k]);
	
	for(int i = 0; i < n; i++)
		tot += a[i];
	
	return tot;
}

int main(void)
{
	int n;
	int a[1000000];
	
	scanf("%d", &n);
	
	printf("%lld\n", sum(a, n));
	
	return 0;
}
